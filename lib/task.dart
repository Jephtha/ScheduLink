class Task {
  String name;
  String courseName;
  String description;
  DateTime dueDate;
  int priority;
  bool isComplete;

  Task(this.name, this.courseName, this.description, this.dueDate, this.priority, this.isComplete);
}